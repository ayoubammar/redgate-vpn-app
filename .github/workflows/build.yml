name: Build RedGate VPN

on:
  workflow_dispatch:

env:
  CHANNEL: prod

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fix all nested submodules iteratively
        run: |
          for i in {1..5}; do
            echo "Iteration $i for fixing submodules..."
            # Replace SSH with HTTPS in any .gitmodules found
            find . -name .gitmodules -exec sed -i 's/git@github.com:/https:\/\/github.com\//g' {} +
            find . -name .gitmodules -exec sed -i 's/ssh:\/\/git@github.com\//https:\/\/github.com\//g' {} +
            
            # Sync the new URLs
            git submodule sync
            
            # Init the immediate submodules ONLY (no --recursive)
            git submodule update --init
          done
          
          # Final sync and full recursive update to be sure
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install dependencies
        run: |
          make get
          make gen || true
          make android-apk-install-deps || true

      - name: Download pre-built core libs
        run: make android-libs CHANNEL=prod

      - name: Download geo assets
        run: make get-geo-assets || true

      - name: Build APK
        run: |
          make android-apk-release CHANNEL=prod || flutter build apk --release --target lib/main.dart

      - name: Find and upload APK
        uses: actions/upload-artifact@v4
        with:
          name: RedGate-VPN-Android
          path: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/apk/**/*.apk

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fix all nested submodules iteratively
        run: |
          for i in {1..5}; do
            echo "Iteration $i for fixing submodules..."
            find . -name .gitmodules -exec sed -i 's/git@github.com:/https:\/\/github.com\//g' {} +
            find . -name .gitmodules -exec sed -i 's/ssh:\/\/git@github.com\//https:\/\/github.com\//g' {} +
            git submodule sync
            git submodule update --init
          done
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install dependencies
        run: |
          make get
          make gen || true
          make windows-install-deps || true

      - name: Download pre-built core libs
        run: make windows-libs CHANNEL=prod

      - name: Download geo assets
        run: make get-geo-assets || true

      - name: Build Windows
        run: |
          make windows-release CHANNEL=prod || flutter build windows --release --target lib/main.dart

      - name: Upload Windows Build
        uses: actions/upload-artifact@v4
        with:
          name: RedGate-VPN-Windows
          path: |
            build/windows/x64/runner/Release/
            dist/
